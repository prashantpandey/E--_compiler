event a(int a2, double a3);
int res;
string s = "\\n";

int abc(int a2, double f) {
    double x = a2 + f;
    int y = a2 + 1;
    int z = a2 + 1;
    double g = a2 + f;

    while (a2 < 1000) {
	x = a2 + f;
	f = f*2 ;
        a2 = a2+100;
    }

    prt -> x;
    prt -> s;

    return a2;
};

a(a2, a3) -> {

   res = abc(a2, a3);

;};
