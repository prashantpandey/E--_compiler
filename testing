event open (string a1 , int a2 , unsigned int a3);

int a;
int b;
int c;

void foo(int h){
    a = h;
    if (a > 1){
	foo(h-1);
    }
}
void abc() {
    int g = 2;
    int h;
    h = 100 * g;
    foo(h);
};

open(file, flags, mode) -> {;};
